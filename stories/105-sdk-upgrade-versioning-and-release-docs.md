# SDK Upgrade, Versioning, And Release Docs

## Scope

- Confirm `createSubscriber` supports email payloads.
- Add wrappers for `disableSubscriber`, optional `deleteSubscriber`, and `disableSubscriberByEmail`.
- Add tests for action mapping and responses.
- Update SDK docs and release notes/version.

## Acceptance

- SDK supports full email subscriber lifecycle.
- Tests catch action-name regressions.
- Upgrade docs are clear for integrators.

## Status

Done (SDK wrappers/tests/docs implemented; version bumped to 0.1.1).
